# Prompt maître v0 — ⚠️ CONTENU MANQUANT

> **Ce fichier est un emplacement vide, pas un document.**
>
> Le brief de session 0 annonçait ce fichier comme « fourni à côté » et comme la
> **source de vérité** du projet. Il est introuvable : recherche par nom de
> fichier et par contenu sur l'ensemble de `C:\Users\marti`, le dossier `noe/`
> étant initialement vide.

## Ce qui est bloqué tant que ce fichier n'est pas rempli

| Artefact | État | Ce qui manque |
| --- | --- | --- |
| `features.json` | **PROVISOIRE** | Les phases réelles du prompt maître. Les 12 features ont été dérivées du brief de session 0 (ordre : spike → format d'épisode → replay fixtures → juge → capture bornée → …) et de la cible Product Hunt. |
| `docs/invariants.md` | **PARTIEL** | Les 5 règles y sont fidèles (elles étaient explicites dans le brief). Les invariants d'architecture et commerciaux sont dérivés, pas extraits. |
| `docs/spike-verdict.md` | **BLOQUÉ** | Les **5 critères de choix du terrain** sont laissés en blanc dans le tableau — ils ne figurent nulle part ailleurs. |
| Session 1 (F01) | **BLOQUÉ** | Le spike a ses « critères dans le prompt maître ». Sans eux, le verdict ne peut pas être rendu. |

## Ce qu'il faut faire

1. Coller ici le contenu intégral du prompt maître v0.
2. Demander la réconciliation :

   ```
   Le prompt maître est déposé dans docs/prompt-maitre-v0.md.
   Réconcilie features.json et docs/invariants.md avec son contenu réel,
   et remplis les 5 critères de terrain dans docs/spike-verdict.md.
   ```

3. Retirer ce bandeau une fois le contenu en place.

---

<!-- ▼▼▼ COLLER LE PROMPT MAÎTRE V0 CI-DESSOUS ▼▼▼ -->
